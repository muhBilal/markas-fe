"use client"
import { Scheduler } from "@aldabil/react-scheduler";

import React from 'react'

const Calendar = () => {
  return (
    <Scheduler
        view="month"
        events={[

        ]}
    />
  )
}

export default Calendar